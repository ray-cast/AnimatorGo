# contrib folders
INCLUDE_DIRECTORIES(${OCTOON_PATH_DEPENDENCIES}/zlib)
INCLUDE_DIRECTORIES(${OCTOON_PATH_DEPENDENCIES}/zipper)

# octoon folder
INCLUDE_DIRECTORIES(${OCTOON_PATH_INCLUDE})

# lib folder for link
LINK_DIRECTORIES(${OCTOON_LIBRARY_OUTPUT_PATH})

OPTION(OCTOON_FEATURE_IO_ENABLE "On for enable off for disable" ON)
OPTION(OCTOON_FEATURE_BASE_ENABLE "On for enable off for disable" ON)
OPTION(OCTOON_FEATURE_INPUT_ENABLE "On for enable off for disable" ON)
OPTION(OCTOON_FEATURE_TIMER_ENABLE "On for enable off for disable" ON)
OPTION(OCTOON_FEATURE_MODEL_ENABLE "On for enable off for disable" ON)
OPTION(OCTOON_FEATURE_GRAPHICS_ENABLE "On for enable off for disable" ON)
OPTION(OCTOON_FEATURE_UI_ENABLE "On for enable off for disable" OFF)
OPTION(OCTOON_FEATURE_VIDEO_ENABLE "On for enable off for disable" ON)

# runtime libraries
ADD_SUBDIRECTORY(octoon-math)
ADD_SUBDIRECTORY(octoon-runtime)
ADD_SUBDIRECTORY(octoon-image)

IF(OCTOON_FEATURE_IO_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_IO_ENABLE)
	ADD_SUBDIRECTORY(octoon-io)
ENDIF()

IF(OCTOON_FEATURE_BASE_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_BASE_ENABLE)
ENDIF()

IF(OCTOON_FEATURE_INPUT_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_INPUT_ENABLE)
	ADD_SUBDIRECTORY(octoon-input)
ENDIF()

IF(OCTOON_FEATURE_TIMER_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_TIMER_ENABLE)
ENDIF()

IF(OCTOON_FEATURE_MODEL_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_MODEL_ENABLE)
	ADD_SUBDIRECTORY(octoon-model)
ENDIF()

IF(OCTOON_FEATURE_GRAPHICS_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_GRAPHICS_ENABLE)
	ADD_SUBDIRECTORY(octoon-graphics)
ENDIF()

IF(OCTOON_FEATURE_VIDEO_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_VIDEO_ENABLE)
	ADD_SUBDIRECTORY(octoon-video)
ENDIF()

IF(OCTOON_FEATURE_UI_ENABLE)
	ADD_DEFINITIONS(-DOCTOON_FEATURE_UI_ENABLE)
	ADD_SUBDIRECTORY(octoon-ui)
ENDIF()

# framework
ADD_SUBDIRECTORY(octoon)
ADD_SUBDIRECTORY(octoon-c)