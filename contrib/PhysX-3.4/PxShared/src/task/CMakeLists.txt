FILE(GLOB_RECURSE HEADERS_LIST ${PHYSX_PATH_SHARED}/src/task/src/*.h ${PHYSX_PATH_SHARED}/src/task/include/*.h)
FILE(GLOB_RECURSE SOURCES_LIST ${PHYSX_PATH_SHARED}/src/task/src/*.cpp)

SOURCE_GROUP("Headers" FILES ${HEADERS_LIST})
SOURCE_GROUP("Sources" FILES ${SOURCES_LIST})

ADD_LIBRARY(PxTask ${HEADERS_LIST} ${SOURCES_LIST})