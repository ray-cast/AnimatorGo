ADD_DEFINITIONS(-DPX_PHYSX_STATIC_LIB)

FILE(GLOB_RECURSE LOWLEVEL_PARTICLES_HEADERS include/*.h)
FILE(GLOB_RECURSE LOWLEVEL_PARTICLES_SOURCES src/*.cpp)

SOURCE_GROUP("Headers" FILES ${LOWLEVEL_PARTICLES_HEADERS})
SOURCE_GROUP("Sources" FILES ${LOWLEVEL_PARTICLES_SOURCES})

ADD_LIBRARY(LowLevelParticles ${LOWLEVEL_PARTICLES_HEADERS} ${LOWLEVEL_PARTICLES_SOURCES})