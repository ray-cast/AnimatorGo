project(imgui C)

SET(LIB_NAME imgui)

FILE(GLOB_RECURSE HEADER_LIST *.h*)
FILE(GLOB_RECURSE SOURCE_LIST *.c*)

SOURCE_GROUP("imgui" FILES ${HEADER_LIST})
SOURCE_GROUP("imgui" FILES ${SOURCE_LIST})

ADD_LIBRARY(${LIB_NAME} STATIC ${HEADER_LIST} ${SOURCE_LIST})