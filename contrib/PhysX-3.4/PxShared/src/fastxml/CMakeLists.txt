ADD_DEFINITIONS(-DPX_FOUNDATION_DLL=0)

FILE(GLOB_RECURSE HEADERS_LIST ${PHYSX_PATH_SHARED}/src/fastxml/src/*.h ${PHYSX_PATH_SHARED}/src/fastxml/include/*.h)
FILE(GLOB_RECURSE SOURCES_LIST ${PHYSX_PATH_SHARED}/src/fastxml/src/*.cpp)

SOURCE_GROUP("Headers" FILES ${HEADERS_LIST})
SOURCE_GROUP("Sources" FILES ${SOURCES_LIST})

ADD_LIBRARY(PsFastXml ${HEADERS_LIST} ${SOURCES_LIST})